# ================================================
# Render 平台环境配置
# ================================================
# Application Port
PORT=22310

# JWT Secret Key - 生产环境使用强随机密钥
# 生成命令：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secure-random-secret-for-render-change-this

# Application URL - Render 自动分配的域名
# TODO: 部署后替换为实际的 Render 域名
NEXT_PUBLIC_APP_URL=https://your-app.onrender.com

# Database Configuration
# Render 持久化存储路径（通过 render.yaml 配置的 disk）
DATABASE_PATH=/data/database.sqlite

# Node Environment
NODE_ENV=production

# ================================================
# Render 部署配置
# ================================================
# Render Deploy Hook URL (用于触发重新部署)
# 获取方式: Render Dashboard > Service Settings > Deploy Hook
RENDER_DEPLOY_HOOK=
