# Render 服务保活方案

## 📋 概述

本文档介绍如何防止 Render 免费实例在无访问时自动休眠的问题，提供三种完全免费的解决方案。

## ⚠️ 问题分析

### 当前 render.yaml 状态

当前配置使用 Render 免费计划（free tier），存在以下特性：

- ✅ 完全免费托管
- ⚠️ **15 分钟无访问后自动休眠**
- ⚠️ 冷启动需要 30-60 秒
- ⚠️ 影响用户首次访问体验

### 休眠机制

```
无访问 15 分钟 → 服务休眠 → 下次访问触发冷启动（30-60s）
```

## 🔧 解决方案

### 方案一：UptimeRobot（⭐⭐⭐⭐⭐ 最推荐）

#### 方案特点

- ✅ 完全免费（支持 50 个监控）
- ✅ 可靠性极高（99.9% uptime）
- ✅ 配置极其简单（3 分钟完成）
- ✅ 提供监控统计和告警
- ✅ 无需修改代码

#### 配置步骤

1. **注册 UptimeRobot**
   - 访问：https://uptimerobot.com/
   - 使用邮箱或 Google 账号注册

2. **创建新监控**
   ```
   Monitor Type: HTTP(s)
   Friendly Name: Kiro Travel
   URL: https://kiro-travel.onrender.com/api/health
   Monitoring Interval: 5 minutes
   ```

3. **高级设置（可选）**
   ```
   Alert Contacts: 添加邮箱接收宕机通知
   HTTP Method: GET
   Timeout: 30 seconds
   ```

4. **验证配置**
   - 等待 5 分钟后查看监控日志
   - 确认服务响应正常

#### 效果

- 每 5 分钟自动访问一次健康检查端点
- 服务永不休眠（比 15 分钟阈值更频繁）
- 提供可视化监控面板

📖 **详细步骤**：参见 [UptimeRobot 设置指南](./13.UptimeRobot设置指南.md)

---

### 方案二：Render Cron Job（⭐⭐⭐⭐）

#### 方案特点

- ✅ 完全免费（Cron Jobs 在免费计划内）
- ✅ 无需第三方服务
- ✅ 配置在 render.yaml 中，易于管理
- ⚠️ Cron Job 本身也可能休眠
- ⚠️ 可靠性略低于 UptimeRobot

#### 配置方法

修改 `render.yaml`，添加 Cron Job 服务：

```yaml
services:
  # 现有的 Web 服务
  - type: web
    name: kiro-travel
    runtime: node
    region: singapore
    plan: free
    branch: master
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_PATH
        value: /data/database.sqlite
      - key: JWT_SECRET
        generateValue: true
      - key: NEXT_PUBLIC_APP_URL
        sync: false
    disk:
      name: data
      mountPath: /data
      sizeGB: 1

  # 新增：保活 Cron Job
  - type: cron
    name: kiro-travel-keepalive
    runtime: node
    region: singapore
    plan: free
    branch: master
    schedule: "*/10 * * * *"  # 每 10 分钟运行一次
    buildCommand: npm install
    startCommand: |
      curl -f https://kiro-travel.onrender.com/api/health || \
      wget -qO- https://kiro-travel.onrender.com/api/health || \
      echo "Ping failed at $(date)"
```

#### Cron 表达式说明

```
*/10 * * * *
 │   │ │ │ │
 │   │ │ │ └─── 星期几 (0-7, 0 和 7 都表示周日)
 │   │ │ └───── 月份 (1-12)
 │   │ └─────── 日期 (1-31)
 │   └───────── 小时 (0-23)
 └─────────── 分钟 (0-59)

示例：
*/10 * * * *  → 每 10 分钟
*/5 * * * *   → 每 5 分钟
0 */1 * * *   → 每小时整点
```

#### 部署步骤

1. 修改 `render.yaml` 文件
2. 提交并推送到 GitHub
3. Render 会自动检测并创建 Cron Job
4. 在 Render Dashboard 中查看 Cron Job 运行日志

---

### 方案三：GitHub Actions（⭐⭐⭐⭐）

#### 方案特点

- ✅ 完全免费（2000 分钟/月额度）
- ✅ 可靠性极高（GitHub 基础设施）
- ✅ 可查看执行历史和日志
- ✅ 支持手动触发
- ⚠️ 需要项目托管在 GitHub
- ⚠️ 配置相对复杂

#### 配置方法

创建 `.github/workflows/keepalive.yml`：

```yaml
name: Keep Render Service Alive

on:
  schedule:
    # 每 10 分钟运行一次（GitHub Actions 最短间隔为 5 分钟）
    - cron: '*/10 * * * *'

  # 允许手动触发
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Service
        run: |
          echo "🚀 Pinging service at $(date)"

          # 尝试访问健康检查端点
          if curl -f -s -o /dev/null -w "%{http_code}" https://kiro-travel.onrender.com/api/health | grep -q "200"; then
            echo "✅ Service is alive (HTTP 200)"
          else
            echo "⚠️ Service returned non-200 status"
            exit 0  # ��失败，因为可能是冷启动
          fi

          echo "✅ Ping completed at $(date)"

      - name: Log Status
        if: always()
        run: |
          echo "Workflow completed with status: ${{ job.status }}"
```

#### 高级配置（可选）

添加通知和监控：

```yaml
name: Keep Render Service Alive with Monitoring

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping-and-monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Service
        id: ping
        run: |
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}:%{time_total}" \
            https://kiro-travel.onrender.com/api/health)

          HTTP_CODE=$(echo $RESPONSE | cut -d':' -f1)
          TIME=$(echo $RESPONSE | cut -d':' -f2)

          echo "http_code=$HTTP_CODE" >> $GITHUB_OUTPUT
          echo "response_time=$TIME" >> $GITHUB_OUTPUT

          echo "📊 Status: $HTTP_CODE | Response Time: ${TIME}s"

      - name: Check Health
        if: steps.ping.outputs.http_code != '200'
        run: |
          echo "⚠️ Warning: Service returned ${{ steps.ping.outputs.http_code }}"
          # 可以在这里添加告警逻辑
```

#### 部署步骤

1. 创建 `.github/workflows/keepalive.yml`
2. 提交并推送到 GitHub
3. 在 GitHub 仓库的 Actions 标签页查看运行状态
4. 可手动触发测试：Actions → Keep Render Service Alive → Run workflow

#### 查看执行日志

```
GitHub Repository → Actions → Keep Render Service Alive → 选择运行记录
```

---

## 📊 方案对比

| 方案 | 成本 | 可靠性 | 配置难度 | 维护成本 | 推荐指数 |
|------|------|--------|----------|----------|----------|
| UptimeRobot | 免费 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ 极简单 | 无 | ⭐⭐⭐⭐⭐ |
| Render Cron Job | 免费 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ 简单 | 低 | ⭐⭐⭐⭐ |
| GitHub Actions | 免费 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ 中等 | 低 | ⭐⭐⭐⭐ |

### 详细评分说明

#### UptimeRobot
- **优势**：专业监控服务，3 分钟配置完成，提供可视化面板
- **适用场景**：所有用户，特别是不想修改代码的用户
- **最佳实践**：首选方案

#### Render Cron Job
- **优势**：集成在 Render 平台，无需外部依赖
- **劣势**：Cron Job 本身可能休眠，可靠性稍低
- **适用场景**：希望所有配置集中在 render.yaml 的用户
- **最佳实践**：与 UptimeRobot 组合使用

#### GitHub Actions
- **优势**：GitHub 基础设施可靠，可查看详细日志
- **劣势**：需要 GitHub 托管，配置相对复杂
- **适用场景**：项目已使用 GitHub，需要监控日志的用户
- **最佳实践**：作为备用方案或与 UptimeRobot 组合

---

## 🎯 推荐实施策略

### 方案 A：单一方案（最简单）

```
选择 UptimeRobot → 5 分钟完成配置 → 长期免维护
```

**适用于**：绝大多数用户

### 方案 B：双重保障（推荐）

```
UptimeRobot（主） + Render Cron Job（备） → 99.99% 可用性
```

**适用于**：对可用性要求较高的项目

### 方案 C：完整监控（专业）

```
UptimeRobot（保活） + GitHub Actions（监控日志） → 完整的监控体系
```

**适用于**：需要详细运行日志的专业项目

---

## 🔍 健康检查端点

��保您的应用有健康检查端点：

### Next.js 示例

创建 `pages/api/health.ts` 或 `app/api/health/route.ts`：

```typescript
// pages/api/health.ts (Pages Router)
import type { NextApiRequest, NextApiResponse } from 'next'

export default function handler(req: NextApiRequest, res: NextApiResponse) {
  res.status(200).json({
    status: 'ok',
    timestamp: new Date().toISOString(),
    uptime: process.uptime()
  })
}
```

```typescript
// app/api/health/route.ts (App Router)
import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({
    status: 'ok',
    timestamp: new Date().toISOString(),
    uptime: process.uptime()
  })
}
```

### 测试健康检查

```bash
# 本地测试
curl http://localhost:3000/api/health

# 生产环境测试
curl https://kiro-travel.onrender.com/api/health
```

预期响应：

```json
{
  "status": "ok",
  "timestamp": "2025-12-16T10:30:00.000Z",
  "uptime": 3600.5
}
```

---

## 📈 监控和验证

### 验证配置是否生效

1. **观察 Render 日志**
   ```
   Render Dashboard → kiro-travel → Logs
   ```
   查找周期性的健康检查请求

2. **检查服务响应时间**
   - 首次访问：应该是即时响应（非冷启动）
   - 间隔 20 分钟后访问：依然快速响应

3. **UptimeRobot 统计**
   ```
   UptimeRobot Dashboard → Monitors → kiro-travel
   ```
   查看 uptime 百分比（应接近 100%）

### 常见问题排查

#### 问题 1：健康检查返回 404

**原因**：健康检查端点不存在

**解决**：
```bash
# 检查路由是否正确
ls pages/api/health.ts
# 或
ls app/api/health/route.ts
```

#### 问题 2：Cron Job 执行失败

**原因**：URL 拼写错误或服务未启动

**解决**：
```yaml
# render.yaml 中检查 URL
startCommand: |
  echo "Testing URL..."
  curl -v https://kiro-travel.onrender.com/api/health
```

#### 问题 3：GitHub Actions 触发频率不符

**原因**：GitHub Actions 可能延迟或跳过某些执行

**说明**：
- GitHub Actions 不保证精确的执行时间
- 高峰期可能延迟 5-10 分钟
- 免费账户可能被限流

**解决**：使用 UptimeRobot 作为主要方案

---

## 💡 最佳实践建议

### 1. 监控间隔设置

```
推荐：5-10 分钟
原因：远小于 15 分钟休眠阈值，留有安全余量

避免：< 5 分钟
原因：过于频繁可能触发 Render 限流
```

### 2. 健康检查端点设计

```typescript
// ✅ 好的实践：轻量级检查
export default function handler(req, res) {
  res.status(200).json({ status: 'ok' })
}

// ❌ 避免：复杂的数据库查询
export default async function handler(req, res) {
  const users = await db.users.findMany() // 不必要的负载
  res.json({ users })
}
```

### 3. 错误处理

```bash
# 为 curl 命令添加容错
curl -f https://your-app.onrender.com/api/health || echo "Failed but continuing"
```

### 4. 日志记录

```typescript
// 记录健康检查请求（可选）
export default function handler(req, res) {
  if (process.env.NODE_ENV === 'development') {
    console.log(`Health check at ${new Date().toISOString()}`)
  }
  res.status(200).json({ status: 'ok' })
}
```

---

## 🚀 快速开始

### 5 分钟快速配置（推荐）

1. **注册 UptimeRobot**（2 分钟）
   - 访问 https://uptimerobot.com/
   - 邮箱注册

2. **创建监控**（2 分钟）
   ```
   URL: https://kiro-travel.onrender.com/api/health
   Interval: 5 minutes
   ```

3. **验证**（1 分钟）
   - 等待第一次检查
   - 确认状态为 "Up"

**完成！** 您的服务现在不会休眠了。

---

## 📚 相关文档

- [UptimeRobot 设置指南](./13.UptimeRobot设置指南.md)
- [Render 部署配置](./render.yaml)
- [GitHub Actions 文档](https://docs.github.com/actions)
- [Render Cron Jobs 文档](https://render.com/docs/cronjobs)

---

## 📝 更新日志

- **2025-12-16**：创建文档，添加三种保活方案
- **推荐方案**：UptimeRobot（最简单可靠）

---

## ❓ 常见问题 FAQ

### Q1: 这些方案会增加成本吗？

**A**: 不会。所有三种方案都完全免费：
- UptimeRobot 免费计划：50 个监控
- Render Cron Jobs：包含在免费计划内
- GitHub Actions：2000 分钟/月免费额度

### Q2: 可以同时使用多个方案吗？

**A**: 可以，推荐 UptimeRobot + Render Cron Job 组合，提供双重保障。

### Q3: 服务会被 Render 限流吗？

**A**: 5-10 分钟的访问频率不会触发限流。Render 限流通常针对每秒多次请求的场景。

### Q4: 如何知道配置成功了？

**A**: 在休眠阈值（15 分钟）后访问您的应用，如果立即响应（无冷启动），说明配置成功。

### Q5: 健康检查会消耗数据库配额吗？

**A**: 不会。推荐的健康检查端点只返回静态 JSON，不访问数据库。

---

**维护者**：Kiro Travel Team
**最后更新**：2025-12-16
**文档版本**：1.0
