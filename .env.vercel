# ================================================
# Vercel 平台环境配置
# ================================================
# Application Port (Vercel 自动分配，此配置仅用于本地测试)
PORT=3000

# JWT Secret Key - 生产环境使用强随机密钥
# 生成命令：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# ⚠️ 重要：请在 Vercel Dashboard 中配置此环境变量
JWT_SECRET=your-secure-random-secret-for-vercel-change-this

# Application URL - Vercel 自动分配的域名
# TODO: 部署后替换为实际的 Vercel 域名
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# Database Configuration
# ⚠️ 重要限制：Vercel Serverless 环境不支持持久化文件系统
# SQLite 文件在每次部署后会丢失，建议改用以下方案：
# 1. Vercel Postgres (推荐): https://vercel.com/docs/storage/vercel-postgres
# 2. Neon Postgres: https://neon.tech/
# 3. Supabase: https://supabase.com/
#
# 如果仅用于演示/测试，可以使用以下路径（数据不持久化）
DATABASE_PATH=/tmp/database.sqlite

# Node Environment
NODE_ENV=production

# ================================================
# Vercel 特定配置（可选）
# ================================================
# 禁用 Vercel Analytics（如果不需要）
# NEXT_TELEMETRY_DISABLED=1
